package rfx.core.stream.data.metric;

public class MetricStreamDataPipe {
	String pubsubChannelName;
	String metricName;//subscribe * from [metric_name] where time
	MetricStreamData metricStream;	
	
}
